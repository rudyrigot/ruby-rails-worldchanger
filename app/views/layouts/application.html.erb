<!DOCTYPE html>
<html>
<head>
  <title>
    <%= @homepage.fragments["product_name"].value %>
    -
    <%= @title %>
  </title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<header class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#top-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to @homepage.fragments["product_name"].value, root_path, :class => "navbar-brand" %>
    </div>
    <div class="navbar-collapse collapse" id="top-navbar">
      <ul class="nav navbar-nav">
        <li <% if @document.try(:id) == api.bookmark('homepage') %>class="active"<% end %>><%= link_to "Home", root_path %></li>
        <li><a href="#">Product tour</a></li>
        <li><a href="#">Pricing</a></li>
        <li><a href="#">FAQ</a></li>
        <li><a href="#">Support</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#">Blog</a></li>
        <li><a href="#">About us</a></li>
      </ul>
    </div><!--/.navbar-collapse -->
  </div>
</header>

<div class="container">
    <% if privileged_access? %>
        <form class="form-inline" role="form" method="GET">
          <div class="form-group">
            <label for="releaseSelector">See this website: </label>
          </div>
          <div class="form-group">
            <select id="releaseSelector" name="ref" onchange="this.form.submit()" class="form-control">
              <% if !api.refs.map{|_,r| r.ref }.include?(current_ref) %>
                <option>?</option>
              <% end %>
              <option value="" <% if current_ref == master_ref %>selected="selected"<% end %>>As currently seen by guest visitors</option>
              <optgroup label="Or preview the website in a future release:">
                <% api.refs.reject{|_,r| r.master? }.each do |_, ref| %>
                  <option value="<%= ref.ref %>" <% if current_ref == ref.ref %>selected="selected"<% end %>>
                    As <%= ref.label %> <%= ref.scheduled_at && ref.scheduled_at.strftime("%Y-%m-%d") %>
                  </option>
                <% end %>
              </optgroup>
            </select>
          </div>
        </form>
    <% end %>
</div>

  <%= yield %>

<hr>

<div class="container">
  <footer>
    <p><%= @homepage.fragments["product_name"].value %> is an example website to be used freely on top of the <a href="http://prismic.io">prismic.io</a> writing-room and content query API.</p>
    <p>
      <% if connected? %>
        <%= link_to signout_path do %>Sign out<% end %>
      <% else %>
        <%= link_to signin_path do %>Sign in to preview changes<% end %>
      <% end %>
    </p>
  </footer>

</div>

</body>
</html>
